## SilenceScore Simple Environment Template
# Usage (single dev workflow):
# 1. cp .env.example .env
# 2. Edit values below once.
# 3. For PRODUCTION build: temporarily set IS_DEVELOPMENT=false & ENABLE_MOCK_SUBSCRIPTIONS=false, run build, then revert.

# Core toggles
IS_DEVELOPMENT=true            # false => production mode (enables strict key validation)
ENABLE_MOCK_SUBSCRIPTIONS=false # true only when testing entitlement logic locally

# Required for production builds (public RevenueCat SDK key)
REVENUECAT_API_KEY=your_revenuecat_public_sdk_key

# Optional services (leave blank to disable)
FIREBASE_API_KEY=
SENTRY_DSN=

# Notes:
# - You do NOT need multiple flavor files; just edit & build.
# - build-prod.sh will still require REVENUECAT_API_KEY (reads via --dart-define, supply env var when invoking).
# - Keep .env out of git (already in .gitignore).
