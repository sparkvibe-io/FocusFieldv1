# RUN FOR ANDROID
flutter clean && flutter pub get

flutter gen-l10n

flutter run -d ZL8322BT88 --dart-define=REVENUECAT_API_KEY=goog_OQmcHbcdgUgLCplNGOzHRVwfqVU --dart-define=ENABLE_MOCK_SUBSCRIPTIONS=false

# RUN FOR IOS
flutter clean && flutter pub get

flutter gen-l10n

cd ios && pod install

cd /Users/krishna/Development/SilenceScore/ios && rm -rf Pods Podfile.lock && sed -i '' "s/platform :ios, '14.0'/platform :ios, '15.0'/" Podfile && pod install


## BUILD FOR PRODUCTION
Android release: 
scripts/build-prod.sh --rc-key=goog_OQmcHbcdgUgLCplNGOzHRVwfqVU

iOS release: 
export REVENUECAT_API_KEY=$REVENUECAT_IOS_API_KEY 
flutter build ipa --release --dart-define=IS_DEVELOPMENT=false --dart-define=ENABLE_MOCK_SUBSCRIPTIONS=false --dart-define=REVENUECAT_API_KEY=$REVENUECAT_API_KEY


PENDING
1. Configure Google Ad Key
2. Create Images
3. Create application page in SparkVibe.io
4. Create Apple account and test out
5. Integrate Apple with Revenue Cat
6. Prepare for deployment