# Example release keystore configuration for SilenceScore
# Copy this file to key.properties (DO NOT COMMIT key.properties)
# Generate a keystore (only once):
# keytool -genkey -v -keystore silencescore-release.keystore -alias silencescore -keyalg RSA -keysize 2048 -validity 10000
# Move the keystore file into android/ (or a secure path you reference below)

storeFile=silencescore-release.keystore
storePassword=CHANGE_ME_STORE_PASSWORD
keyAlias=silencescore
keyPassword=CHANGE_ME_KEY_PASSWORD
